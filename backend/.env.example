# ======================================================================
# DEEP FOCUS PLANNER - BACKEND ENVIRONMENT CONFIGURATION
# ======================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit your .env file to version control!
#
# Generate a secure SECRET_KEY with:
#   python -c "import secrets; print(secrets.token_hex(32))"
# ======================================================================

# ----------------------------------------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------------------------------------
# SQLite (Development - Default)
# DATABASE_URL=sqlite:///./planner.db

# PostgreSQL (Production - Recommended)
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://planner_user:planner_password@localhost:5432/planner_db

# Production example (use your actual credentials):
# DATABASE_URL=postgresql://prod_user:secure_password@db.example.com:5432/planner_prod

# ----------------------------------------------------------------------
# SECURITY & AUTHENTICATION
# ----------------------------------------------------------------------
# Secret key for JWT token signing - MUST be changed in production!
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production-use-openssl-rand-hex-32

# JWT algorithm (HS256 is standard, don't change unless you know what you're doing)
ALGORITHM=HS256

# JWT token expiration time in minutes (default: 10080 = 7 days)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ----------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# ----------------------------------------------------------------------
# Comma-separated list of allowed origins (no spaces!)
# Development:
ALLOWED_ORIGINS_STR=http://localhost:3000,http://localhost:3001,http://localhost:8000

# Production example:
# ALLOWED_ORIGINS_STR=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com

# ----------------------------------------------------------------------
# FRONTEND URL
# ----------------------------------------------------------------------
# Base URL of your frontend application (used for redirect links in emails and OAuth)
# Development:
FRONTEND_URL=http://localhost:3000

# Staging:
# FRONTEND_URL=https://staging.yourdomain.com

# Production:
# FRONTEND_URL=https://yourdomain.com

# ----------------------------------------------------------------------
# EMAIL SERVICE CONFIGURATION
# ----------------------------------------------------------------------
# Choose your email provider: 'sendgrid' or 'smtp'
EMAIL_PROVIDER=sendgrid

# --- SendGrid Configuration (Recommended for Production) ---
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# See EMAIL_SETUP.md for detailed setup instructions
SENDGRID_API_KEY=

# Production example:
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxx.yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy

# --- SMTP Configuration (Good for Development/Gmail) ---
# For Gmail, you need to create an "App Password":
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification
# 3. Go to App Passwords and generate one for "Mail"
# See EMAIL_SETUP.md for detailed instructions

# Gmail SMTP:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Other SMTP providers:
# Outlook: smtp-mail.outlook.com:587
# Yahoo: smtp.mail.yahoo.com:587
# Custom SMTP: your-smtp-server.com:587

# Email sender information (appears in "From" field)
EMAIL_FROM=noreply@deepfocusplanner.com
EMAIL_FROM_NAME=Deep Focus Planner

# ----------------------------------------------------------------------
# OAUTH AUTHENTICATION - GOOGLE
# ----------------------------------------------------------------------
# Google OAuth 2.0 credentials from Google Cloud Console
# Setup guide: See SETUP_OAUTH.md for detailed instructions
#
# Required redirect URIs to configure in Google Cloud Console:
# Development: http://localhost:3000/auth/google/callback
# Production: https://yourdomain.com/auth/google/callback

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Example (DO NOT USE - get your own from Google Cloud Console):
# GOOGLE_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-abcdefghijklmnopqrstuvwxyz

# ----------------------------------------------------------------------
# OAUTH AUTHENTICATION - APPLE
# ----------------------------------------------------------------------
# Apple Sign In credentials from Apple Developer Portal
# Setup guide: See SETUP_OAUTH.md for detailed instructions
# Note: Requires Apple Developer Account ($99/year)
#
# Required redirect URIs to configure in Apple Developer Portal:
# Development: http://localhost:3000/auth/apple/callback (requires special setup)
# Production: https://yourdomain.com/auth/apple/callback

APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

# Example format (DO NOT USE - get your own from Apple Developer):
# APPLE_CLIENT_ID=com.yourdomain.deepfocusplanner
# APPLE_TEAM_ID=ABCDE12345
# APPLE_KEY_ID=FGHIJ67890
# APPLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQg...\n-----END PRIVATE KEY-----

# ----------------------------------------------------------------------
# TOKEN EXPIRY SETTINGS
# ----------------------------------------------------------------------
# Email verification token expiry (in hours)
EMAIL_VERIFICATION_EXPIRE_HOURS=24

# Password reset token expiry (in hours)
PASSWORD_RESET_EXPIRE_HOURS=1

# ----------------------------------------------------------------------
# ENVIRONMENT
# ----------------------------------------------------------------------
# Application environment: 'development', 'staging', or 'production'
ENVIRONMENT=development
